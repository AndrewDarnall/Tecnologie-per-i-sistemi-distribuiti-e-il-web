CREATE TABLE events (
                        id int NOT NULL PRIMARY KEY,
                        name varchar(256),
                        location varchar(256),
                        date varchar,
                        description varchar

);